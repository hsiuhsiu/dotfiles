- defaults:
    link:
      relink: true
      create: true

- clean: ["~"]

- link:
    ~/.gitignore_global: git/gitignore_global
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.cmqlstyle.css: cmqlstyle.css
